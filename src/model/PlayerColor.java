package model;

/**
 * 
 * Enum a játékos színek reprezentálására.
 * Minden játékosnak egy színe lesz, ezért felfogható azonosítóként is
 */

public enum PlayerColor {
	USER, RED, GREEN, BLUE, YELLOW
}
